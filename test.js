function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
function innerFunction() {
    debugger; // Ini adalah breakpoint di dalam fungsi innerFunction()
    // Kode lain di dalam innerFunction()
}
